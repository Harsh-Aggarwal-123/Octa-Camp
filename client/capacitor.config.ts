import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.octacamp.app',
  appName: 'octa-camp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
